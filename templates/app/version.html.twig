{% if lastVersion is not null %}
<a href="javascript:void(0);">Version {{ lastVersion.number }}</a><br><small>Build {{ lastVersion.build }} - {{ lastVersion.date }}</small>
{% else %}
Version << undefined>>
{% endif %}